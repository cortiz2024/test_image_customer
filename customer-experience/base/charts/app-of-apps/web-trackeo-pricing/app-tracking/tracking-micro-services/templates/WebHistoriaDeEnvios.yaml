apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: {{ .Values.appThree.appName }}-{{ .Values.environment }}
  namespace: argocd
  finalizers: {{ .Values.appThree.finalizers | toYaml | nindent 4 }}
spec:
  destination: 
    server: {{ .Values.appOne.destination.server }}
  project: project-{{ .Values.appThree.projectName }}-{{.Values.environment}}
  source:
    path: {{ .Values.appThree.source.path }}
    repoURL: {{ .Values.appOne.source.repoURL }}
    targetRevision: {{ .Values.appOne.source.targetRevision }}
  syncPolicy:
    automated: 
      prune: true
      allowEmpty: true
      selfHeal: true
