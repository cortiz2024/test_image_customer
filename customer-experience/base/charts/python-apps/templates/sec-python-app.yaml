apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: {{ .Values.appTwo.appName }}-{{ .Values.environment }}
  namespace: argocd
  finalizers: {{ .Values.appTwo.finalizers | toYaml | nindent 4 }}
  labels:
    name: {{ .Values.appTwo.appName }}-{{ .Values.environment }}
spec:
  destination:
    namespace: {{ .Values.namespace }}-{{ .Values.environment }}
    server: {{ .Values.appTwo.destination.server }}
  project: {{ .Values.appTwo.projectName }}-{{ .Values.environment }}
  source:
    chart: {{ .Values.appTwo.source.chartName }}
    repoURL: {{ .Values.appTwo.source.repoURL }}
    targetRevision: {{ .Values.appTwo.source.targetRevision }}
    helm:
      releaseName: {{ .Values.appTwo.appName }}-{{ .Values.environment }}
  syncPolicy:
    automated: 
      prune: true
      allowEmpty: true
      selfHeal: true
